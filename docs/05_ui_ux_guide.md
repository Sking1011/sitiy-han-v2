# UI/UX Дизайн-система

## Стиль
- **Минимализм:** Чистые отступы, отсутствие лишних теней и градиентов.
- **Цветовая схема:**
  - Светлая тема: Белый фон (#FFFFFF), светло-серые акценты, основной текст темно-серый.
  - Темная тема: Глубокий темно-серый/черный фон, контрастный светлый текст.
- **Компоненты:** Использование Shadcn UI для обеспечения единообразия.

## Эталон верстки страниц (Dashboard Standard)

Все страницы приложения должны соответствовать единой структуре отступов и типографики, где эталоном является главная страница (Дашборд).

### 1. Контейнер страницы
Основной оберточный элемент (`main`) уже имеет паддинги в `layout.tsx`. Внутри `page.tsx` контент должен оборачиваться в:
- `space-y-6`: Вертикальный отступ между крупными блоками (Шапка -> Статистика -> Таблица).

### 2. Заголовок и Подзаголовки
Шапка страницы всегда оформляется как `flex-col` блок:
- **Обертка:** `<div className="flex flex-col gap-2">`
- **Заголовок (H1):** `text-2xl font-bold lg:text-3xl tracking-tight`
- **Подзаголовок (P):** `text-muted-foreground text-sm md:text-base`

### 3. Сетка карточек и Элементы
Для обеспечения "воздушности" интерфейса:
- **Сетка (Grid):** `grid gap-4 md:grid-cols-2 lg:grid-cols-4` (или `lg:grid-cols-7` для сложных схем).
- **Карточки:** Всегда `rounded-xl` (12px), `border`, `bg-card`, `shadow-sm`.
- **Внутренние отступы карточек:** `p-4 sm:p-6`.

### 4. Цвета и Акценты
- **Primary:** Используется для кнопок действий и акцентов в заголовках.
- **Muted:** Для второстепенной информации.
- **Индикаторы:** 
  - Успех (Закуп/Доход): `text-green-600`
  - Ошибка/Убыток (Списание): `text-red-600`
  - Процесс: `text-orange-500`

### 5. Отступы от краев (Safe Zones)
- **Десктоп:** Отступ от Sidebar и Header контролируется общим лейаутом.
- **Мобайл:** Нижний отступ `pb-32` обязателен для предотвращения перекрытия контента плавающей навигацией (BottomNav).

## Мобильный UI/UX (Mobile-First)

Поскольку 80% работы ведется с телефона, применяются следующие паттерны:

### 1. Отказ от таблиц
На мобильных экранах таблицы (`<table>`) заменяются на список **Карточек (`<Card>`)**.
- **Состав карточки:**
  - Заголовок (Крупно).
  - Статус (Цветная полоска/бейдж).
  - Ключевые данные (Остаток, Цена).
  - Быстрые действия (Кнопки "Изменить", "В корзину").

### 2. Навигация и FAB (Floating Action Button)
- **Нижнее меню:** Содержит 4 основных раздела + Центральную кнопку действия.
- **Кнопка [ + ]:** Вызывает модальное окно с быстрыми действиями (Продать, Закупить, Произвести). Позволяет совершить операцию из любой точки приложения.

### 3. Режим "Цех" (Cook Mode)
Для страницы просмотра рецептов:
- Увеличенный шрифт.
- Чек-боксы для ингредиентов (отмечать добавленное).
- **Wake Lock:** Блокировка затухания экрана телефона.

### 5. Модуль "Бухгалтерия" (Accounting)
- **Фильтры:** Период (Месяц, Год, Произвольный).
- **Таблица P&L (Отчет о прибылях и убытках):**
  - Выручка (+)
  - Себестоимость (-)
  - Операционные расходы (-)
  - **Прибыль до налогов (=)**
  - Налог (напр. 3%) (-)
  - **Чистая прибыль (=)**
- **Экспорт:** Кнопка "Скачать Excel (.xlsx)". Файл содержит 3 листа: "Движение денег", "Продажи", "Склад".

### 6. Формы расходов
- Поле **"Источник оплаты"** (Switch/Radio):
  - 🏢 **Бизнес Касса** (Спишется с баланса приложения).
  - 👤 **Личные средства** (Баланс приложения не изменится, но расход учтется).

### 4. Главная страница (Дашборд)
- **Главный индикатор:** Виджет **"Свободные деньги"**.
- **Финансовый блок:** Переключатель "До налогов" / "После налогов".

## Адаптация под Роли

### Интерфейс Оператора
- **Скрыто:**
  - Все виджеты с деньгами.
  - Колонки "Цена" и "Сумма" в таблицах.
  - Разделы "Продажи", "Закупки" (если оператор не закупает), "Аналитика", "Настройки".
- **Видно:**
  - Склад (только остатки в кг/шт).
  - Кнопка "Производство" (Создать партию).

### Интерфейс Директора/Админа
- Доступна кнопка **"Журнал действий"** (иконка 🛡️ или 📜) в шапке.
- При редактировании товара поле "Цена" доступно для изменения.
- **Приоритет:** Сначала "Критические уведомления" (заканчивается сырье) и "Сводка дня".
- **Вторично:** Графики и диаграммы (доступны по скроллу вниз).

## Работа с единицами измерения (Smart Units)
Система должна скрывать от пользователя математику дробных чисел базовых единиц.

1. **Ввод данных (Smart Input):**
   - Поля ввода количества (в рецептах, производстве) должны иметь переключатель единиц (КГ <-> Г, Л <-> МЛ).
   - Пример: Пользователь вводит "500" и выбирает "г". Система сохраняет `0.5`.

2. **Отображение (Smart Display):**
   - **Авто-форматирование:**
     - Если значение < 1 (базовой ед.), отображать в младших единицах (0.4 кг -> **400 г**).
     - Если значение >= 1, отображать в старших единицах (1.2 кг -> **1.2 кг**).
   - **В рецептах:** Всегда отображать в тех единицах, которые удобны технологу (обычно граммы для специй).
